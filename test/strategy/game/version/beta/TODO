XStart a game
XMove a piece
XRed wins a battle
XRed loses a battle
XRed wins the game
XRed loses the game
XDraw after max turns
XDon't allow invalid board
XFirst, on the initial configuration, Red places pieces in the bottom two rows (0,0)..(5,0) and (0, 1)..(5,1) and  
	Blue places pieces in the top two rows (0, 4)..(5,4) and (0,5)..(5,5).
XNull if no battle or draw
XResulting location if won
XUpdate Alpha Strategy tests to use the strategy factory
XMax turns is 6 each

